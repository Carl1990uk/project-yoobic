// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  firebase: {
    projectId: 'yoobic-project',
    appId: '1:1077389455943:web:7e127e6a98c1a190edb762',
    storageBucket: 'yoobic-project.appspot.com',
    locationId: 'europe-west2',
    apiKey: 'AIzaSyCHdbkHUQ9CWy5brkj8bNkBWTpRYvZmo9A',
    authDomain: 'yoobic-project.firebaseapp.com',
    messagingSenderId: '1077389455943',
    measurementId: 'G-9ZPP28JTQR',
  },
  production: false
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
